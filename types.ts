export interface BoardMember {
  name: string;
  title: string;
  image_url: string;
  linkedin_url: string;
  bio: string;
}

export interface LeaderboardEntry {
  rank: string;
  name: string;
  points: string;
  change: string;
}
